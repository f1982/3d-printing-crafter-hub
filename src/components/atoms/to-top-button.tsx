'use client'

import ScrollToTop from 'react-scroll-to-top'

export default function ToTopButton() {
  return (
    <ScrollToTop
      smooth
      className="flex w-9 items-center justify-center bg-primary"
    />
  )
}
